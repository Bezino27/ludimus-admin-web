services:
  admin_web:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-https://atukosice.sk}
    container_name: ludimus_admin_web
    restart: always
    ports:
      - "4173:80"
